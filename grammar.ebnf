program ::= { statement } exit_stmt
statement ::= decl | assign
decl ::= "i32" [ "mut" ] ident "{" expr "}"
assign ::= ident ":=" expr
exit_stmt ::= "exit" factor
expr ::= term { ( "+" | "-" ) term }
term ::= factor { "*" factor }
factor ::= number | ident
